# Force using the same architecture for all packages
force-platform=linux
# Increase network timeout for slow connections
network-timeout=100000
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

